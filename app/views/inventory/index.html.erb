<h1 class="text-center">INVENTORY</h1>
<ul>
  <% @items.each do |item| %>
    <section class="movie">
      <div class="image">
        <%= image_tag(item.image_file_name)%>
      </div>
      <div class="summary">
        <h1>
          <%= link_to item.name, item, style: "color: #793969;" %>
        </h1>
        <h3>
          <p>
            Price: <%= item.price %>
          </p>
          <p>
            MG: <%= item.mg_percent %>
          </p>
        </h3>
      </div>
    </section>
  <% end %>
  <section class="admin">
    <%= link_to "Add New Item", new_inventory_path, class: "button" %>
  </section>
</ul>